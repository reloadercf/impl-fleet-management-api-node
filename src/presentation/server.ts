import express from 'express';
import path from 'path';

interface Options{
    PORT: number;
    PUBLIC_PAth: string;
}
class Server {
    public static start() {
        console.log('Server started...');
        
    }
}